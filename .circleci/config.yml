version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build-and-test:
    executor:
      name: node/default
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: npm install
            - run: ls -ll
            # - run: npm test
  deploy:
    # Install npm dependencies 🙂
    # - run: npm install
    # !!! This installs AWS Elastic Beanstalk CLI 'awsebcli' !!!
    executor:
      name: node/default
    steps:
      - checkout
      - run:
          name: list current folder
          command: ls -ll
          #name: Installing deployment dependencies
          #command: |
          #  sudo apt-get -y -qq update
          #  sudo apt-get install python-pip python-dev build-essential
          #  sudo pip install --upgrade setuptools
           # sudo pip install awsebcli --upgrade
      # !!! This runs a deployment
     # - run:
       #   name: Deploying
      #    command: |
       #     eb init --platform 'Node.js 10 running on 64bit Amazon Linux 2/5.0.1' --region eu-central-1 SnowsLightshop
        #    eb deploy Snowslightshop-env
          # eb init --platform <platform-name> --region <region-name> <application-name>

workflows:
  build-and-test:
    jobs:
      - build-and-test
      - deploy:
          filters:
            branches:
              only:
                - master
                - circleci-project-setup
